services:
  mobrob:
    build:
      context: ..
      dockerfile: dockerfiles/basicdriveDockerfile
    pull_policy: build
    network_mode: host
    stdin_open: true
    tty: true
    privileged: true
    volumes:
      - /dev:/dev